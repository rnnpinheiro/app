// arrays, objetos

let metas = [2, "Alô"];  // colchetes [ ], dentro do array, índice 0 1 2 etc. as vzs não se sabe oq tem no array
console.log(metas[1] + ", " + metas[0]);

// Operador de concatenação